# nixpacks 配置:明确指定 Python 构建和启动命令
[start]
cmd = "python app.py"

[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["python -m venv /opt/venv", "/opt/venv/bin/pip install -r requirements.txt"]

[variables]
PATH = "/opt/venv/bin:/usr/local/bin:/usr/bin:/bin"
